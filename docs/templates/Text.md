```cpp
    Text* myText = new Text(100, 100, 500); // x,y,width
    myText->setText("Stay Funky FNF"); // text lol
    myText->setFormat("assets/fonts/5by7.ttf", 36, 0xFFFFFFFF); // font path, size, color
    engine.addText(myText); // adds the text
```